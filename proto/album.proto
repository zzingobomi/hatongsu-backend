syntax = "proto3";

package album;

service AlbumService {        
    rpc UploadImage(stream ImageUploadRequest) returns (UploadResponse);        
}

message ImageUploadRequest {
    string filename = 1;
    bytes content = 2;
}

message UploadResponse {
    string message = 1;
}